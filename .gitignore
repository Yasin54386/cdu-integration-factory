# NOTE: generated/ is deliberately NOT ignored — the pipeline commits it
# back to feature branches (spec §4).
__pycache__/
*.pyc
.venv/
venv/
.env
.pytest_cache/
*.egg-info/

# Local clone of the target MuleSoft repo that Copilot edits in place
# (never committed to the factory branch; pushed to its own remote instead).
mule_workspace/
